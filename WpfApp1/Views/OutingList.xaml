<UserControl x:Class="WpfApp1.Views.OutingList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label x:Name="infoList" Grid.Row="1" Content="Click on the list for more info/ for reservation" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <DataGrid x:Name="gridOuting" AutoGenerateColumns="False" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Width="*"/>
                <DataGridTextColumn Header="Starting Hour" Width="*"/>
                <DataGridTextColumn Header="Starting Point" Width="*"/>
                <DataGridTextColumn Header="Category" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
